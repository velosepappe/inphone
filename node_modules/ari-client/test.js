'use strict';

var timeout = setTimeout(() => {
    console.log('timeout reached');
}, 1000);

for(let x=0; x<10000000000;x++) {
    clearTimeout(timeout);
}
console.log('great big loopy loop done');
